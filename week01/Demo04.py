# 随机生成一个包含1000个字母的字符串，然后统计该字符串中每个字母的数量，
# 并输出结果（要求结果以字典方式存储）（20分：随机生成字符串5分，
# 统计字母数量8分，以字典方式存储5分，输出结果2分）
import random
b = ""
# 随机生成一个包含1000个字母的字符串s
for i in range(1001):
    a = random.randrange(a,z)
    b += a
print(b)